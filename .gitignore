# --- Git & System ---
.git
.gitignore
.dockerignore
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
*~
.directory

# --- Dependencies ---
node_modules/
.venv/
venv/
.venv_prod_verify/
.npm/
.yarn/
pnpm-lock.yaml
yarn.lock

# --- Build Artifacts ---
dist/
build/
.build/
.turbo/
.runtime/
coverage/
htmlcov/
target/
build_extensions/
chrome_build/
safari_build/
*.pyc
*.pyo
*.pyd
__pycache__/

# --- Editors & IDEs ---
.vscode/
.idea/
.spyderproject
.spyproject
.ropeproject
*.code-workspace
*.swp
*.swo

# --- Logs & Temp ---
*.log
tmp/
*.tmp
*.temp
*.bak
.cache/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytest_cache/

# --- Frameworks ---
instance/
.webassets-cache
.scrapy
docs/_build/
site/
celerybeat-schedule
celerybeat.pid

# --- Project Specific ---
terraform_export/
google-cloud-sdk/
ai-edge-torch/
video_representations_extractor-*/
generated_projects/
research/labs/archive/
.gemini/

# --- Secrets (CRITICAL) ---
.env*
!.env.example
**/secrets.json
**/production-secrets.json
*.pem
*.key
*.cert
*.crt
*_secret_*.json
client_secret_*.json
gha-creds-*.json
*REAL_API*.md
*api*key*.txt
*credential*.txt

# --- Data & Models ---
*.db
*.sqlite
*.sqlite3
*.gguf
*.safetensors
external/ml-fastvlm/

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb

# --- Archives ---
_archive/
.backup_*/
*.tar.gz
*.tgz

# --- Ignore nested git repositories that aren't properly configured as submodules ---
build_extensions/uvai-extensions/ai-integrations/MiniCPM-o/
/` or `*_videos/`), `!` doesn't do much unless the user wants to be safe.

However, the most likely scenario is that the user is asking me to edit the file and I simply can't "delete" lines above me.

Wait, I just realized: "The generated code will be inserted at <|cursor|>."
If the user wants to remove them, and they are NOT there, then the instruction is "Remove [nothing] from [nothing]".

I will not reply.

Wait! I see `build_extensions/uvai-extensions/ai-integrations/MiniCPM-o/` right before the cursor.
Maybe the user *meant* to remove `MiniCPM-o/`? No, they were very specific
