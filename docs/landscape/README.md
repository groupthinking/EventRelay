# Landscape track (side branch)

See [WATERFALL.md](./WATERFALL.md) for gates.

Workflows:

- `/wdk-product-waterfall` — product gates (B / 3b / C)
- `/uvai-source-brief family=eventrelay` — one family
- `/uvai-landscape-research` — all families (`mode=full` adds propose+skeptic)

Artifacts from a run land in the workflow scratch, not on `main`.
