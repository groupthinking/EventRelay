#!/usr/bin/env bash
# RETIRED: direct local Cloud Run deployment bypasses exact-SHA, migration,
# identity, secret-version, smoke-test, and rollback gates.
echo "RETIRED: use the protected Deploy Cloud Run PostgreSQL substrate workflow." >&2
exit 1
