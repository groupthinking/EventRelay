## 2025-07-12 - Unit Tests

**Learning:** There appear to be thousands of failing tests in the test suite that are unrelated to the current task (Weight Persistence testing). Given the sheer volume (1995 failed), this seems like preexisting tech debt. The tests for `test_weight_persistence.py` run perfectly.

**Action:** Logged failures but proceeding as the specific task (testing Weight Persistence) has been fully and successfully completed.
