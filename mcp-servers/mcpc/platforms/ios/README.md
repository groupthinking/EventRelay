# MCPC iOS Platform

Mobile-specific hand-off point for the unified MCPC server. Code previously tracked in `MCP_IOS` is relocated here so mobile flows can be routed from the canonical MCP entry point.
