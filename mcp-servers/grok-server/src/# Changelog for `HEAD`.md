# Changelog for `HEAD`
> Generated by Gemini 2.5 Flash Preview 05-20 from 84 commits between `HEAD` and `temp-staging-fixes`


Use the 👍 and 👎 buttons in the editor toolbar to provide feedback on this AI response. *Your feedback helps us improve our AI features.*

----

### Added
- Adds Cloud Run worker configuration, including Cloud SQL integration, resource limits, and production environment setup.
- Adds a script to deploy the UVAI worker service to Cloud Run.
- Enables asynchronous processing for improved performance.
- Adds new VS Code extension recommendations and GitLens launchpad indicator color setting.
- Adds a `benchmark_and_commit` tool with CLI execution mode.
- Adds agent invocation limits and tracking within `AgentConfig` and `AgentManager`.
- Upgrades the system to Level 7: Fleet Commander, adding recursive scanning and exclusion capabilities.
- Upgrades the system to Level 6: CI/CD Integration for Autonomous Optimization.
- Upgrades the system to Level 5: Autonomous Contributor tools and client.
- Adds Autonomous Auditor capabilities to the MCP profiling server.
- Adds a safe TDD optimization workflow, including backups and parity checks.
- Implements a staging area for robust MCP self-optimization persistence.
- Adds MCP self-optimization persistence and a GitHub Action workflow.
- Adds a `submit_patch` tool for hot-swapping code and updates the client workflow.
- Adds an MCP profiling server and investigator agent concept.
- Adds NoteGPT flowchart and JSONC style guide documentation.
- Implements a vector search and embedding API using Cloudflare AI and Vectorize, and adds new D1 database bindings.
- Introduces Cloudflare, YouTube UVAI, and State Coordinator MCP servers, and updates agent and connector logic.
- Introduces code analysis subagents.
- Adds new documentation and debug files.
- Adds project review links to documentation.
- Migrates and adds 18 documentation files to a new shared directory, including progress logs, monitoring guides, and project decisions.

### Changed
- Significantly optimizes the `slow_fibonacci` function, achieving up to a 63745.1x speedup.
- Renames project resources to UVAI and standardizes service, topic, and subscription names across the project.
- Updates Google Cloud project ID and service account references across frontend, service, and setup configurations.
- Refactors the project folder structure to reduce overlap, consolidate `OpenAI_Hub` content to the root, flatten the overall structure, and resolve nested repository issues. ([#42](https://github.com/groupthinking/EventRelay/issues/42))
- Improves patch submission robustness and introduces a verification script.
- Updates project dependencies and structure, archiving old modules and removing files from the main repository.
- Cleans up the development directory structure, archiving legacy folders, consolidating `state-fabric-concept`, and organizing unassigned files.
- Exempts `/api/vector` routes from CSRF validation and owner-only authentication to support external API integration needs.
- Updates numerous npm and yarn dependencies across various modules. ([#41](https://github.com/groupthinking/EventRelay/issues/41), [#40](https://github.com/groupthinking/EventRelay/issues/40))

### Removed
- Removes archived Genkit MCP full repository files and the old Genkit repository, resolving security alerts.
- Removes old virtual environment files, the `a2ui` submodule, and the D-Wave quantum connector.
- Removes the archived MCP bridge.
- Removes outdated CodeViz context diagram and related files.
- Removes numerous legacy and archived files, including one from the `mcp-supabase-frontend` subproject.

### Fixed
- Fixes API startup errors by moving and enabling service container initialization and correcting imports in legacy endpoints to use relative paths.
- Fixes MCP workflow to correctly use the requirements file and improves pip caching.
- Fixes script path resolution in `investigator_client.py`.
- Fixes MCP client session initialization and Python executable path.
- Fixes `package-lock.json` for Continuous Integration.
- Fixes deployment issues related to npm dependencies, routes, and build configuration.
- Fixes broken submodules.

### Security
- Implements a dedicated service account with least-privilege IAM roles.
