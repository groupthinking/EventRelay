# Production Environment Configuration
# Canonical domain: uvai.io
# API backend: api.uvai.io (points to Cloud Run service)

# Production backend URL — Cloud Run service
# This is the default for production builds (Vercel, etc.)
# Override with Vercel environment variables if needed.
BACKEND_URL=https://uvai-backend-gpwz4wb5na-uc.a.run.app
NEXT_PUBLIC_BACKEND_URL=https://uvai-backend-gpwz4wb5na-uc.a.run.app

# Canonical URLs
NEXT_PUBLIC_APP_URL=https://uvai.io
NEXT_PUBLIC_API_URL=https://api.uvai.io
