# RETIRED: this entrypoint intentionally fails closed. The protected GitHub
# workflow is the only deployment path because it migrates PostgreSQL first.
steps:
  - name: alpine:3.22
    entrypoint: sh
    args:
      - -ec
      - |
        echo "RETIRED: use .github/workflows/deploy-cloud-run.yml" >&2
        exit 1
