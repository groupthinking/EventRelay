#!/usr/bin/env bash
# RETIRED: infrastructure and IAM are managed out-of-band and deployment runs
# only through .github/workflows/deploy-cloud-run.yml.
echo "RETIRED: use the protected Deploy Cloud Run PostgreSQL substrate workflow." >&2
exit 1
